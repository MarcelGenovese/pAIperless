Analyze this document and extract metadata in JSON format.

Document Text:
{{DOCUMENT_CONTENT}}

Respond with JSON:
{
  "tags": ["tag1", "tag2"],
  "filename": "suggested-filename",
  "correspondent": "Sender Name",
  "custom_fields": {
    "action_description": "What user needs to do",
    "due_date": "YYYY-MM-DD"
  },
  "action_required": true/false
}

Instructions:
- Determine if user action is required (payment deadline, cancellation period, response needed, etc.)
- Extract relevant tags based on document type and content
- Suggest a descriptive filename following the pattern: YYYY-MM-DD_correspondent_description
- Identify the correspondent (sender, organization, company)
- If action is required, specify what needs to be done and when
- Use ISO date format (YYYY-MM-DD) for due dates
- Be concise and accurate
